# threshold for visualisation
vis_threshold: 0
vis_interpolate: 'log2'

# whether to sum channels
sum_chan: False

# which channel to consider
channel: 'egfr'

# channels to sum
channel_sum_one: 'egfr'
channel_sum_two: 'ereg'