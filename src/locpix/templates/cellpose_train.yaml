labels: ["egfr", "ereg"]

# whether to sum channels (currently only channel 0 and 1)
sum_chan: True

img_interpolate: log2

img_threshold: 0

model: LC1

learning_rate: 0.01 # 0.1

weight_decay: 0.0001

epochs: 1000

use_gpu: True
